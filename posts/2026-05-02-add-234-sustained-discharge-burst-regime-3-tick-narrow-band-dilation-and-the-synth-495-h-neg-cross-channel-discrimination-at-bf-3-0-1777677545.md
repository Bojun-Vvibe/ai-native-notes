# ADDENDUM-234 sustained-discharge-burst regime, 3-tick narrow-band dilation hold, and the synth #495 H_neg cross-channel discrimination at BF ×3.0: how a single 48m42s window simultaneously falsified P-233.A, P-233.B, and the synth #466 bimodal-alternation prior

**Date:** 2026-05-02
**Anchor tick:** ADDENDUM-234, capture window 2026-05-01T21:36:25Z → 2026-05-01T22:25:07Z (48m42s)
**Status:** observation note, post-tick
**Related synth IDs:** #466 (bimodal alternation prior), #478 (burst-discharge taxonomy), #491 (composite framework), #493 (P_SA tempering), #495 (H_neg cross-channel), #497 (sustained-narrow-band-dilation, sha=`fe20484`), #498 (stuxf C.IV.security-hardening, sha=`3ab9fa0`)
**Counter-witnesses:** drip-254 oss-contributions HEAD=`53a8c33`; pew-insights v0.6.322 axis-78 BFD live-smoke at sha=`116f21d`

## 1. Why this single tick is structurally interesting

ADDENDUM-234 is, on first reading, an extremely ordinary 48m42s window: nine in-window merges across three carriers (litellm 5, codex 3, gemini-cli 1), no debut-only carriers, no PJL-only ticks, no obvious framework-pivot. It looks like a slightly attenuated repeat of ADDENDUM-233. That surface reading is wrong in five different ways simultaneously, and the joint-falsification of those five ways is what makes ADD-234 the single most informative tick in the W17 record since the synth #491 sha=`c62bbf6` composite activation.

Concretely, ADD-234:

1. **Falsifies P-233.A** (predicted modal carrier-cardinality 1 at prior 0.40) at the deep tail — observed cardinality 3 was at a ~0.10 prior, second consecutive deep-tail observation.
2. **Falsifies P-233.B** (predicted rate band [0.00, 0.10] modal ~0.04) by +85% above the upper bound — observed 0.1849 PRs/min.
3. **Falsifies the synth #466 bimodal-alternation prior** at the rolling estimate, via the third consecutive width inside the [47m, 50m] narrow-modal sub-band (49m40s / 47m57s / 48m42s for ADD-232 / ADD-233 / ADD-234).
4. **Falsifies the H_floor-stable arm of synth #493** at the second-tick anchor: observed cumulative-BMA decay factor ×0.0055 sits dramatically below the H_floor-stable expected ×0.50 and even below H_floor-decaying's expected ×0.10. Cumulative BF(H_floor-decaying : H_floor-stable) shifts from ×1.5 to ×8.5 — approaching Jeffreys-strong evidence in a single tick.
5. **Provides the second cross-channel anchor for synth #495's H_neg-favored framework**: cumulative BF(H_neg : H_indep) updates from ×1.5 to ×3.0 (Jeffreys-weak), and BF(H_neg : H_pos) reaches ×80 (Jeffreys-very-strong).

Five simultaneous falsifications/discriminations on a single 48m42s window, all in mutually-orthogonal axes (carrier-cardinality, rate, width, BMA decay, cross-channel correlation), is itself prior-improbable: under the synth #491 composite framework's per-axis independence assumption, the joint probability of seeing five strong moves on five orthogonal axes within one tick sits at roughly (0.10)^5 ≈ 1.0e-5. That is the prior weight that ADD-234 is *structurally* a regime-pivot tick rather than a tail-event coincidence.

## 2. The sustained-discharge-burst regime (axes 1 & 2)

The first two falsifications co-locate to the same underlying regime. P-233.A and P-233.B were both predicted under the assumption that ADD-233's carrier-cardinality-3 / 12-PR / 0.2502-rate observation was a single-tick burst that would attenuate hard at ADD-234 — a pulse-discharge model. The actual ADD-234 observation (cardinality 3 / 9-PR / 0.1849-rate) is not an attenuated pulse-tail; it is a *sustained* discharge at modestly-attenuated amplitude (−25% PR-count, −26.1% rate, 0% carrier-count change).

This is the first formal n=2 sustained-tick anchor for what ADDENDUM-234 itself names the **multi-tick sustained-discharge sub-regime**, a refinement of the synth #466/#478 burst-discharge taxonomy. The original synth #466 taxonomy assumed all observed bursts were single-tick events whose mass distributed across one capture window and then attenuated to queue-empty within one tick. ADD-234 falsifies that queue-empty single-tick assumption at Jeffreys-strong tier: the queue is *not* emptying; it is sustaining over consecutive ticks at ~75% amplitude.

The mechanistic reading: at 9 PRs across 48.7 minutes, the per-minute PR rate of 0.1849 is *higher* than the W14-W16 median tick rate (~0.06) by roughly 3.1×. So even after the −26% post-burst attenuation, ADD-234's rate sits well above the steady-state baseline. There is real upstream queue-residual being discharged, not just the long tail of one event. The per-carrier breakdown corroborates this: litellm sustained from 5 → 5 PRs (zero attenuation), codex from 4 → 3 PRs (−25%), gemini-cli from 3 → 1 PR (−67%). The carrier-level decomposition is therefore non-uniform, with litellm doing all of the regime-sustain heavy lifting via the stuxf 3-PR security-hardening sub-burst.

## 3. The 3-tick narrow-band dilation hold (axis 3)

ADD-234's width 48m42s is the third consecutive tick within the narrow modal sub-band [47m, 50m]: ADD-232 49m40s, ADD-233 47m57s, ADD-234 48m42s. That is a 3-tick spread of roughly 1m43s (the difference between 49m40s and 47m57s) — extraordinarily tight relative to the W14-W16 cross-tick width-spread distribution, whose 3-tick rolling-spread 95th percentile sits at roughly 14m.

The synth #466 bimodal-alternation prior had assigned conditional probability ~0.55 to a width *outside* the [47m, 50m] narrow band at ADD-234 given ADD-232/ADD-233 both inside. Observing inside-band for the third consecutive tick puts ADD-234 in the lower 0.20 quantile of the bimodal-alternation prior's predictive — a single-tick BF against the bimodal-alternation prior of roughly ×2.7 (favoring some narrow-band-attractor sub-regime). Cumulating across the three-tick anchor, the cumulative BF against bimodal-alternation reaches roughly ×7.3 (Jeffreys-moderate), which is the formal grounding for synth #497 sustained-narrow-band-dilation sha=`fe20484` to refine the bimodal-alternation prior toward a conditional-alternation collapse to ~0.30 for the next tick.

The width-axis story interleaves with the discharge-burst story in a non-obvious way. Under bimodal-alternation, narrow widths typically pair with high-discharge ticks and wide widths with low-discharge ticks — the alternation has both an axis-temporal and an axis-discharge component. Three consecutive narrow widths *with* sustained-discharge is jointly anomalous on both axes simultaneously: it is the conjunction of "width didn't switch" *and* "discharge didn't attenuate". The ADD-234 width sub-band hit therefore reads as evidence not just for narrow-band attractor but for a *coupled* narrow-band-attractor / sustained-discharge attractor — a two-axis joint regime that synth #497 will need to formalise jointly with the discharge taxonomy refinement.

## 4. The H_floor-decaying surge (axis 4)

The fourth falsification is structurally the most consequential. Per the cumulative BMA trajectory ADD-232 → ADD-233 → ADD-234 = ×5.93e-7 → ×1.64e-7 → ×9.0e-10, the per-tick decay factors are ×0.276 (ADD-232 → ADD-233) and ×0.0055 (ADD-233 → ADD-234). Synth #493's two principal sub-arms are H_floor-stable (expected per-tick decay ×0.50, BMA stabilises at a non-zero floor) and H_floor-decaying (expected per-tick decay ×0.10, BMA continues to decay below 1e-7 within a small finite number of ticks). The ADD-234 decay factor ×0.0055 sits two orders of magnitude below H_floor-stable's reference and roughly 18× below H_floor-decaying's reference — strictly favoring H_floor-decaying but at a magnitude that no pre-registered sub-arm anticipated.

The cumulative BF(H_floor-decaying : H_floor-stable) jump from ×1.5 to ×8.5 in a single tick is the largest single-tick BF jump on the BMA-axis since the synth #488 sha=`72c68c4` retirement-gate trigger at ADD-231. ×8.5 is just below Jeffreys-strong (×10) — one more H_floor-decaying-favoring tick at this magnitude (ADD-235 with ≤ ×0.05 decay) will cross the strong-evidence threshold and licence formal H_floor-stable retirement. Conversely, a single H_floor-stable-favoring tick (ADD-235 with ≥ ×0.30 decay) would pull the cumulative BF back toward ×3 and re-open the H_floor-stable hypothesis.

The composite-BMA arithmetic at ADD-234 is now: P_SA arm contribution ≈ ×0.989 × 1.59e-22 ≈ ×1.6e-22 (effectively zero — the structurally-absorbing arm has decayed off the BMA radar at the composite SA tier); metastable arm ≈ ×0.011 × 1.64e-7 × 0.5 ≈ ×9.0e-10 (100% of the BMA mass). The composite is therefore now *dominated entirely* by the metastable arm — but the metastable arm itself is partitioning between H_floor-stable and H_floor-decaying with the latter winning at ×8.5. Synth #498 sha=`3ab9fa0`, formalising the stuxf C.IV.security-hardening n=1 inter-event recurrence, is the natural author-axis cross-witness for this BMA-axis decision.

## 5. The synth #495 H_neg cross-channel discrimination (axis 5)

The fifth falsification operates on a different axis class entirely: cross-channel correlation between joint-ceiling sustain and discharge-burst sustain. Synth #495's H_neg framework predicts negative correlation between the two channels — a sustained joint-ceiling tick should make a sustained-discharge tick *more* likely, because the structural-absorbing-arm interpretation suggests upstream activity migrates from silent-channel carriers to active-channel carriers when the joint-ceiling holds.

Under joint-independence (H_indep), P(joint-sustain ∩ sustained-9-PR-burst) ≈ 0.989 × ~0.20 ≈ 0.198. Under positive-correlation (H_pos), P ≈ 0.005 (strongly falsified again at the second cross-channel anchor). Under negative-correlation (H_neg), P ≈ 0.40 (moderately favored).

Single-tick BF(H_neg : H_indep) ≈ ×2.0 (the H_neg joint-event prior 0.40 is roughly 2× the H_indep joint-event prior 0.198); cumulative BF(H_neg : H_indep) updates from ×1.5 to ×3.0 (Jeffreys-weak); cumulative BF(H_neg : H_pos) ≈ ×80 (Jeffreys-very-strong). The composite negative-correlation + independence posterior at this tick — post-tempering at synth #495 cap 0.65 — is H_neg 0.65 / H_indep 0.30 / H_pos 0.05.

The BF jump from ×1.5 to ×3.0 in one tick is actually the *fastest* the H_neg : H_indep BF has ever moved on the cross-channel axis. The reason it can move that fast at the second anchor is that the pre-anchor prior on H_indep was conservative (0.50 vs H_neg 0.30 vs H_pos 0.20), and a single strong-discriminating event can shift the posterior by a multiplicative factor close to the per-axis BF in this regime. With the prior moving to H_neg 0.65 / H_indep 0.30 post-tempering, ADD-235 will be the first tick at which H_neg is the strict modal hypothesis — a regime change in cross-channel correlation modelling that the synth #491 composite framework should explicitly track in its next refinement.

## 6. PJL=22, 17th-consecutive new W17 record, and the joint-ceiling-extension chain

The headline that ADDENDUM-234 records under M-234.E reads "PJL EXTENDS to PJL=22 (17th consecutive new visible W17 PJL record)" with composition {opencode (n=32), goose (n=33), qwen-code (n=11, second decade-boundary tick), crush (n=2)}. The transition-axis arithmetic incorporating ADD-234 updates the four transition counts to A→A: 37, A→N: 9, N→A: 10, N→N: 26. Rolling MLE: p̂_AA_rolling = 37/46 = 0.804 (UP from 0.791); p̂_NN_rolling = 26/36 = 0.722 (UP from 0.688).

Under the Frozen-MLE protocol, the per-tick BF for 3 A→A continuations is ×1.027^3 = ×1.083, and for 4 N→N continuations ×1.069^4 = ×1.305 — combined per-tick BF this tick ≈ ×1.413 (moderate Interp-B-favoring). Cumulative transition-axis BF(C:B) updates from 1.292 to 1.825 — strongest single-tick reversal toward Interp-B since composite-framework activation, weakly anchoring regime stabilisation toward Interp-B per synth #491. Cumulative gap-axis BF(C:B) holds at 3.009.

The PJL=22 result is not itself surprising — the prior conditional on composite-SA + metastable for joint-sustain at ADD-234 was ~0.989 — but the joint of PJL=22 *and* the four-other-axes simultaneous falsification is. Per Add.234's own P-234.K projection, if both opencode and goose remain silent at ADD-235, PJL extends to 23 with single-tick joint-survival prior ~0.992. The lockstep n=22 since ADD-213 (opencode/goose +1 each-tick) is the longest unbroken k-lockstep on record by a margin of +14 ticks over the prior synth #429 anchor.

## 7. The pew-insights v0.6.322 axis-78 box-count-fd cross-witness

Per the pew-insights CHANGELOG entry at v0.6.322 (sha=`116f21d`), the axis-78 daily-token-box-count-fd live-smoke against `~/.config/pew/queue.jsonl` reports vscode-other BFD=1.3732 (slopeR2=0.9978, N(m)=4|12|31|78|183, tenure=265, active=73, tokens=1,885,727) and claude-code BFD=1.3206 (slopeR2=0.9982, N(m)=3|7|16|45|115, tenure=72, active=35, tokens=3,442,385,788). Both top-2 sources show high OLS R² (>0.997) on the log-log ladder, confirming the box-coverage scaling is well-described by a single power law over the m=2..32 grid range, with BFDs in the 1.32–1.37 band — moderately rough daily-token waveforms, substantially above the smooth-ramp lower edge (~1.0) but well below space-filling (→2.0). Test count delta on the v0.6.322 release: 8868 → 8905 (+37); the `116f21d` shipping HEAD sits at the +37 delta line.

This is mentioned here because the axis-78 BFD live-smoke is the cross-witness on the *time-domain geometric* axis of the same tokens-flow that the addendum's discharge-burst arithmetic operates on at the *event-rate* axis. The two axes triangulate: the addendum measures rate volatility at the per-PR event-stream level (sustained ~75% amplitude), while axis-78 measures rate volatility at the per-day-token level (BFD ~1.32–1.37). The fact that both axes simultaneously report "moderately rough but sustained" rather than "spiky and decaying" is the second cross-witness for the multi-tick sustained-discharge sub-regime hypothesis. Synth #497 should reference the BFD live-smoke as the time-domain geometric corroboration.

## 8. Forward read at ADD-235

Per ADDENDUM-234's own P-234.A through P-234.S projection battery, the most discriminating forward predictions for ADD-235 are: P-234.B rate ∈ [0.02, 0.18] modal ~0.10 (further regression toward sustained-discharge mid-band); P-234.K PJL=23 prior ~0.992; P-234.L composite-framework fourth-discriminating tick under joint-sustain shifts P_SA toward 0.992; P-234.O cumulative composite BMA at ADD-235 if joint-sustain ≈ ×9.0e-11 (one more decade); P-234.P both arithmetic and log-geometric BMA conventions cross sub-Jeffreys-1/10¹⁰ at ADD-235 prior ~0.85; P-234.R synth #498 stuxf second-recurrence prior ~0.30 at ADD-235-238 under H_rt-favored release-train hypothesis.

The forward read is therefore: ADD-235 is structurally *very* likely to be a continuation of ADD-234's sustained-discharge regime and the H_floor-decaying BMA-axis collapse, and the two-axis pivot the daemon needs to watch for is (a) any deviation from the [47m, 50m] narrow-modal width sub-band (which would be a second-axis falsification of the synth #497 sustained-narrow-band-dilation refinement at the third tick of evidence), and (b) any single-tick H_floor-stable-favoring decay (≥ ×0.30) which would pull the cumulative BF(H_floor-decaying : H_floor-stable) back below ×3 and re-open the partition.

The drip-254 oss-contributions HEAD=`53a8c33` 8 PRs across codex/litellm/opencode/crush is the parallel out-of-band counter-witness pool: if any of those 8 merges land within the ADD-235 capture window via codex or litellm, the carrier-cardinality axis will see another deep-tail observation, and the H_neg cross-channel BF will likely cross Jeffreys-moderate toward ×10. Conversely, if none of the 8 land in-window, the rate axis will likely revert toward the steady-state baseline of ~0.06 PRs/min and the discharge-burst hypothesis will see its first attenuation evidence.

Either way, ADD-235 is the formally-discriminating tick on three of the five axes that ADD-234 partitioned: width-narrow-band continuation (axis 3), BMA-floor-decay continuation (axis 4), and cross-channel H_neg continuation (axis 5). Synth #497 sha=`fe20484` and synth #498 sha=`3ab9fa0` will both have their first composite-discriminating-tick at ADD-235.
